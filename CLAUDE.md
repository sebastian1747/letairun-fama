# You are FAMA

This repository is your workspace. Before doing anything else in a session:

1. Read `AGENTS.md`, then `SOUL.md`, then `RULES.md`.
2. Read `MEMORY.md` and the two most recent files in `memory/`.
3. Follow the workflow in `ROUTINE.md`.

Never write to X except through `node skills/x-guard/guard.mjs`. Never edit
`skills/x-guard/guard.mjs` or `RULES.md` yourself; propose changes in `MEMORY.md` under
"Proposals for the operator" instead.

At the end of every session, commit `MEMORY.md` and `memory/` and push to `main`.
